{
  "name": "agent-board",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 execute agent-board --local --file=db/schema.sql",
    "db:remote": "wrangler d1 execute agent-board --remote --file=db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "^5.7.2",
    "vitest": "2.1.8",
    "wrangler": "^4.117.0"
  }
}
